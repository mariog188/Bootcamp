import { StringEmojiPipe } from './string-emoji.pipe';

describe('StringEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new StringEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
